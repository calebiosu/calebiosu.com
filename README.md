<h1>Calebiosu.com</h1>

<h2>Installation</h2>

To install run the following command in the project root:
> `npm run init`

This will:

- Install package dependencies via `npm install`


<h2>Startup</h2>

To start the app, use the command:
> `npm run start`